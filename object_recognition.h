#pragma once // جلوگیری از چندبار include شدن فایل هدر (جلوگیری از خطای تکرار تعریف)
// اجرای تشخیص شی روی چند تصویر
// folder : پوشه تصاویر (مثلاً inputs)
// count  : تعداد تصاویر
void objectRecognition(const char* folder, int count);
